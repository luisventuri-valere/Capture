// Staffing tab — rebuilt as the rich 8-section Staffing module.
// Implementation lives in ./staffing/StaffingScreen. Re-exported here so the
// existing App.tsx import path (`./components/capture/StaffingTab`) stays valid.
export { StaffingScreen as StaffingTab } from './staffing/StaffingScreen';
